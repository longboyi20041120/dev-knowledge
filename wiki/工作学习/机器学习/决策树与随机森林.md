---
tags:
  - "#用途/工作学习"
  - "#类型/技术"
  - "#技术/机器学习"
  - "#状态/草稿"
created: 2026-06-26
updated: 2026-06-26
status: draft
---

# 决策树与随机森林

面试中最常考的两个非深度学习模型。原理直观，业务上用得最多。

## 一、决策树：三个核心概念

### 信息熵（衡量不确定性）

```
熵 H = -Σ p_i × log₂(p_i)

例子：
- 全是正例（p=1.0）：熵 = 0（完全确定）
- 一半正一半负（p=0.5）：熵 = 1（最不确定）
```

### 信息增益

分裂前的熵 - 分裂后的加权平均熵。**信息增益越大，说明这个特征越适合用来分裂。**

### 三个分裂标准

| 标准 | 原理 | 特点 |
|------|------|------|
| ID3（信息增益） | 分裂后熵减少最多 | 偏向取值多的特征 |
| C4.5（信息增益率） | 信息增益 / 特征本身的熵 | 修正了 ID3 的偏向 |
| CART（基尼系数） | Gini = 1 - Σ p_i² | sklearn 默认，计算快 |

## 二、从零构建决策树

```python
import numpy as np

def gini(y):
    """基尼系数：越小越纯"""
    _, counts = np.unique(y, return_counts=True)
    probs = counts / len(y)
    return 1 - np.sum(probs ** 2)

def best_split(X, y):
    """穷举所有特征和阈值，找到基尼系数最小的分裂"""
    best_gini = float("inf")
    best_feature, best_threshold = None, None

    for feat in range(X.shape[1]):
        thresholds = np.unique(X[:, feat])
        for thresh in thresholds:
            left = y[X[:, feat] <= thresh]
            right = y[X[:, feat] > thresh]
            if len(left) == 0 or len(right) == 0:
                continue
            g = (len(left) * gini(left) + len(right) * gini(right)) / len(y)
            if g < best_gini:
                best_gini = g
                best_feature = feat
                best_threshold = thresh

    return best_feature, best_threshold
```

## 三、过拟合与剪枝

决策树最大的问题：**容易过拟合**。如果不限制，树会一直分裂到每个叶子只有一个样本。

```python
from sklearn.tree import DecisionTreeClassifier

# 不限制 → 过拟合（训练集 100%，测试集 70%）
dt = DecisionTreeClassifier()

# 加约束 → 泛化好
dt = DecisionTreeClassifier(
    max_depth=5,           # 最大深度
    min_samples_split=10,  # 节点最少样本数才继续分裂
    min_samples_leaf=5,    # 叶子最少样本数
    max_features="sqrt",   # 每次分裂只考虑 sqrt(n) 个特征
)
```

## 四、随机森林：多棵树投票

**原理**：训练 N 棵决策树，每棵树只用部分样本（Bootstrap）和部分特征（随机选），最后投票决定结果。

```python
from sklearn.ensemble import RandomForestClassifier

rf = RandomForestClassifier(
    n_estimators=100,      # 100 棵树
    max_depth=10,
    random_state=42
)
rf.fit(X_train, y_train)

# 特征重要性（面试常问：怎么判断哪些特征重要）
for name, importance in zip(feature_names, rf.feature_importances_):
    print(f"{name}: {importance:.3f}")
```

**为什么随机森林比单棵决策树好？**

① **Bagging 降低方差**：每棵树用不同数据训练，取平均后噪声互相抵消
② **随机特征选择降低相关性**：如果每棵树都一样，投票没意义；特征随机保证树之间有差异
③ **方差低 = 过拟合风险小**

## 五、决策树 vs 线性模型

| | 决策树/随机森林 | 线性回归/逻辑回归 |
|------|------|------|
| 特征关系 | 自动捕捉非线性 | 只能学线性关系 |
| 特征工程 | 不需要标准化 | 需要 |
| 可解释性 | 树结构直观（特征重要性） | 系数直接表示影响 |
| 小数据 | 容易过拟合 | 更稳定 |
| 大数据 | 随机森林训练慢（100棵树） | 快 |
| 典型场景 | 表格数据分类、推荐系统 | 评分卡、风控 |

**面试话术**："线性模型假设线性关系，树模型自动学非线性。不知道数据什么关系就先上随机森林，效果通常不错。需要解释每个特征怎么影响结果就用逻辑回归。"

## 六、面试常见追问

**Q: 随机森林为什么随机？两个随机分别在哪？**

A: ① 样本随机：每棵树 Bagging 采样（有放回抽样约 63% 样本) ② 特征随机：每次分裂只考虑部分特征（通常 sqrt(n)）。两个随机让树之间多样化。

**Q: GBDT 和随机森林的区别？**

A: 随机森林 Bagging（并行训练多棵树取平均），GBDT Boosting（串行训练，每棵新树修正前一棵的残差）。Boosting 偏差更低但更容易过拟合。

## 相关笔记

- [[机器学习/线性回归与梯度下降]]
- [[机器学习/模型评估与调参]]（待写）
