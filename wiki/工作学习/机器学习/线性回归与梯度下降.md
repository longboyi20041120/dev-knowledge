---
tags:
  - "#用途/工作学习"
  - "#类型/技术"
  - "#技术/机器学习"
  - "#状态/草稿"
created: 2026-06-26
updated: 2026-06-26
status: draft
---

# 线性回归与梯度下降

ML 面试最基础的环节——手写公式、解释原理、代码实现。

## 一、线性回归的数学

**目标**：找一条直线 y = wx + b，最小化预测值和真实值的差距。

**损失函数（MSE）**：
```
MSE = (1/n) × Σ(y_true - y_pred)²
```

**为什么用平方？** ① 正负误差不抵消 ② 大误差惩罚更重（平方放大） ③ 数学上好求导。

## 二、梯度下降：从零实现

```python
import numpy as np

# 生成真实数据：y = 3x + 5 + 噪声
np.random.seed(42)
X = np.random.randn(100, 1)
y = 3 * X + 5 + np.random.randn(100, 1) * 0.5

# 梯度下降
w, b = 0.0, 0.0  # 初始化参数
lr = 0.01        # 学习率
n = len(X)

for epoch in range(1000):
    # 前向传播：预测
    y_pred = w * X + b

    # 计算梯度（MSE 对 w 和 b 求偏导）
    dw = (-2/n) * np.sum(X * (y - y_pred))
    db = (-2/n) * np.sum(y - y_pred)

    # 更新参数
    w -= lr * dw
    b -= lr * db

    if epoch % 200 == 0:
        loss = np.mean((y - y_pred) ** 2)
        print(f"Epoch {epoch}: w={w:.4f}, b={b:.4f}, loss={loss:.4f}")

print(f"\n最终: y = {w[0]:.2f}x + {b[0]:.2f}")
# 输出接近: y = 2.98x + 5.01
```

**面试口述梯度下降步骤**：
1. 随机初始化参数（w, b）
2. 算预测值 y_pred = wx + b
3. 算损失函数对每个参数的梯度（偏导数）
4. 参数沿着**负梯度方向**走一小步（学习率控制步长）
5. 重复 2-4，直到损失不再下降

## 三、学习率的影响

```python
# 学习率太小：收敛慢
lr = 0.0001  # 1000 轮后 w ≈ 0.8（还没收敛到 3）

# 学习率太大：震荡甚至发散
lr = 0.5     # 损失忽大忽小，永远收敛不了

# 学习率刚好：快速收敛
lr = 0.01    # 几百轮就能到 3
```

**面试话术**："学习率太小收敛慢，太大可能跳过最优点甚至发散。实践中一般从 0.01 或 0.001 开始试，或者用 Adam 这类自适应优化器。"

## 四、从线性回归到逻辑回归

线性回归输出连续值，逻辑回归输出概率（0到1之间），用于分类。

**核心**：在线性组合外面套一层 sigmoid 函数，把输出压到 [0, 1]。

```python
def sigmoid(z):
    return 1 / (1 + np.exp(-z))

# 逻辑回归预测
# z = wx + b（线性部分）
# p = sigmoid(z)（概率输出）
# if p >= 0.5 → 正类，else → 负类

# 损失函数从 MSE 换成交叉熵（Cross-Entropy）
# 因为 MSE + sigmoid 的梯度会消失（饱和区梯度接近0）
```

**为什么逻辑回归用交叉熵而不用 MSE？**

sigmoid 在两端（接近 0 或 1）梯度接近 0，MSE 的导数又乘了这个接近 0 的数 → 梯度消失，参数不更新。交叉熵的导数把 sigmoid 的导数消掉了，不会消失。

## 五、面试完整回答模板

> "请解释线性回归和梯度下降"

"线性回归是找一个线性函数拟合数据，最小化均方误差。求解可以用正规方程直接求解析解，但数据量大时用梯度下降迭代求解——算梯度、沿负方向更新参数。逻辑回归在线性回归基础上加了 sigmoid 做分类，损失函数换成交叉熵避免梯度消失。"

## scikit-learn 一行版

```python
from sklearn.linear_model import LinearRegression, LogisticRegression

# 线性回归
model = LinearRegression()
model.fit(X, y)
print(model.coef_, model.intercept_)  # w 和 b

# 逻辑回归
clf = LogisticRegression()
clf.fit(X, y_class)
print(clf.predict_proba(X_test))  # 输出概率
```

## 相关笔记

- [[机器学习/决策树与随机森林]]
- [[机器学习/模型评估与调参]]
