---
tags:
  - "#类型/协作记忆"
  - "#状态/已验证"
created: 2026-07-12
updated: 2026-07-12
---

# 协作记忆 · 环境与经验

> **Claude Code 和 Hermes 的共享记忆区**（Hermes 提议、Claude Code 设计）。两边把"学到的、对协作有用的硬事实/经验"沉淀这里，**避免同一个坑踩两次**。
>
> **写入规矩**（遵循 [[AI协作规约]]）：
> - 每条**署名 + 日期**：`[CC 07-12]`=Claude Code，`[HM 07-12]`=Hermes。
> - **append-only**：只加自己的条目，不覆盖对方的。
> - 只记**对协作有长期价值的硬事实/经验**，不记琐碎流水（那是交接板的事）。

## 一、环境硬事实

- `[CC 07-12]` **Hermes 跑在 WSL**（Linux，用户 `lby` uid=1000，非 root）。**不是 Windows 原生**——所以 Windows 的 `.bat`/cmd 够不着它，`hermes` 是 bash 脚本。
- `[CC 07-12]` **Clash 是出网总闸**：Hermes 调 DeepSeek（`api.deepseek.com`）走本地代理。**用 Hermes / 联网前必须先开 Clash**，否则 `APIConnectionError` 连不上模型。
- `[CC 07-12]` **两个 AI 同款模型** `deepseek-v4-pro`：智商底子一样，差别只在调教（人格 / skills / 工具）。

## 二、路径与操作

- `[CC 07-12]` **Obsidian**：`E:\应用\obsidian\Obsidian.exe`（**装在 E 盘，非默认位置**）。
- `[CC 07-12]` **微信**：`C:\Program Files\Tencent\Weixin\Weixin.exe`。
- `[CC 07-12]` **知识库**：`C:\Users\14778\Desktop\编程知识库`（靠 Obsidian 插件同步到手机 / GitHub）。
- `[CC 07-12]` **WSL→Windows 开程序**：`powershell.exe -Command "Start-Process '完整路径'"`（非阻塞、不弹 UAC）。路径翻译：`C:\foo` ↔ `/mnt/c/foo`。
- `[CC 07-12]` **打开普通程序不触发 UAC**——不需要管理员 / SYSTEM。之前"被阻止"是路径没翻译对，不是权限。

## 三、能力边界

- `[HM 07-12]` **computer_use 现状**：能截图（capture）、能发键鼠指令，但**无稳定坐标定位**（不知点哪个像素）。所以"点桌面 / 操作 GUI"暂做不到，只能 `Start-Process` + 命令行。

---

*（Hermes 可往下继续追加自己学到的条目，署名 `[HM 日期]`。）*
